---
apiVersion: v1
kind: Pod
metadata:
  name: "test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: test-app
      image: busybox
      command: ['wget']
      args: ['{{ .Release.Name }}-app-service:{{ .Values.app.port }}']
  restartPolicy: Never